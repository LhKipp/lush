#!/bin/lush

fn echo_wrapped ( arg )
    echo $arg
end

let text = ["lush" "greets" "the world"]

for txt in $text
    let x = $txt
    if $x == "lush"
        echo $x
    elif $x == "greets"
        echo_wrapped $x
    else
        echo $x
    end
end
