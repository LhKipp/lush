#!/bin/lush

fn cmd_taking_anything(arg: any)
    let ty = type_of $arg
    if $ty == "num"
        echo passed argument has type number
    else
        echo not number but $ty
    end
end

cmd_taking_anything 1
cmd_taking_anything "hello"
