#!/bin/lush

fn echo_wrapped ( arg )
    echo $arg
end

let text = ["lush" "greets" "the world"]

for txt in $text
    let x = $txt
    if $x == "hello"
        echo $x
    elif $x == "world"
        echo_wrapped $x
    else
        echo $x
    end
end
